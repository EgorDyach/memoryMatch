import * as localGameSelectors from "./selectors";

export {
  name as localGameStoreName,
  reducer as localGameReducer,
  actions as localGameActions,
} from "./store";

export { localGameSelectors };
